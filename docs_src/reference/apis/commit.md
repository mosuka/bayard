# Commit API

Commit API commits updates made to the index.

## Request

```text
GET /index/commit
```

## Example

To commit an index:

```text
$ curl -X GET 'http://localhost:8000/index/commit'
```
