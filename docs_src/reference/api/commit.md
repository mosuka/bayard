# Commit API

Commit API commits updates made to the index.

## Request

```text
GET /v1/commit
```

## Example

To commit an index:

```shell script
$ curl -X GET 'http://localhost:8000/v1/commit'
```
